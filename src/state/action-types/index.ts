export enum ActionType {
  DEPOSIT = "deposit",
  WITHDRAW = "withdraw",
  BANKRUPT = "bankrupt",
  ADD_WINDOW = "ADD_WINDOW",
  REMOVE_WINDOW = "REMOVE_WINDOW",
  CREATE_BANK = "CREATE_BANK",
  CALCULATE = "CALCULATE",
}
